<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.SocialPostMapper">

    <resultMap id="SocialPostResultMap" type="com.community.entity.SocialPost">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.community.entity.SocialPost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO social_post (user_id, content)
        VALUES (#{userId}, #{content})
    </insert>

    <select id="selectById" resultMap="SocialPostResultMap">
        SELECT id, user_id, content, created_at, updated_at
        FROM social_post
        WHERE id = #{id}
    </select>

    <select id="selectFeed" resultMap="SocialPostResultMap">
        SELECT id, user_id, content, created_at, updated_at
        FROM social_post
        <where>
            <if test="beforeId != null">
                id &lt; #{beforeId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>

