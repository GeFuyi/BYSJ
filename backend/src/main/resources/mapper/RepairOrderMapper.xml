<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.RepairOrderMapper">

    <resultMap id="RepairOrderResultMap" type="com.community.entity.RepairOrder">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="handlerId" column="handler_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.community.entity.RepairOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO repair_order (user_id, handler_id, title, description, contact_phone, status)
        VALUES (#{userId}, #{handlerId}, #{title}, #{description}, #{contactPhone}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE repair_order
        SET status = #{status},
            handler_id = #{handlerId},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="RepairOrderResultMap">
        SELECT id, user_id, handler_id, title, description, contact_phone, status, created_at, updated_at
        FROM repair_order
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="RepairOrderResultMap">
        SELECT id, user_id, handler_id, title, description, contact_phone, status, created_at, updated_at
        FROM repair_order
        <where>
            <if test="status != null and status != ''">
                status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectByUserId" resultMap="RepairOrderResultMap">
        SELECT id, user_id, handler_id, title, description, contact_phone, status, created_at, updated_at
        FROM repair_order
        <where>
            user_id = #{userId}
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>

