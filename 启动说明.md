# BYSJ 本地启动说明

## 1. 前置环境

| 组件 | 版本 | 安装方式 |
|---|---|---|
| JDK | 17 | `brew install openjdk@17` |
| Maven | 3.9+ | `brew install maven` |
| MySQL | 8.0 | `brew install mysql@8.0` |
| Redis | 7+ | `brew install redis` |
| Node.js | 18+ | 已有 |
| npm | 9+ | 已有 |

## 2. 环境变量

以下变量需要在终端 export 或写入 `~/.zshrc`。

### 2.1 Java / Maven（已加入系统变量则跳过）

```bash
export PATH="/opt/homebrew/bin:/opt/homebrew/opt/openjdk@17/bin:$PATH"
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"
```

### 2.2 数据库

```bash
export COMMUNITY_DB_PASSWORD=
```

说明：Homebrew 安装的 MySQL root 默认空密码，所以设为空。如果你改过 root 密码，填实际密码。

### 2.3 阿里云短信

```bash
export ALIBABA_CLOUD_ACCESS_KEY_ID=你的阿里云AccessKeyID
export ALIBABA_CLOUD_ACCESS_KEY_SECRET=你的阿里云AccessKeySecret
```

短信签名和模板 Code 已写入项目 `application.yml` 默认值：

```yaml
sign-name: 速通互联验证码
template-code: 100001
scheme-name: 测试方案
```

如需覆盖可设置：

```bash
export SMS_SIGN_NAME=你的签名
export SMS_TEMPLATE_CODE=你的模板Code
```

### 2.4 支付宝

```bash
export ALIPAY_ENABLED=true
export ALIPAY_APP_ID=你的支付宝AppID
export ALIPAY_PRIVATE_KEY=你的支付宝应用私钥
export ALIPAY_PUBLIC_KEY=你的支付宝公钥
export ALIPAY_NOTIFY_URL=你的支付宝回调地址
```

如果本地不需要电费代缴功能，可以设：

```bash
export ALIPAY_ENABLED=false
```

### 2.5 从 Windows 导出文件读取变量（可选）

环境变量导出文件路径：

```text
/Users/jason/Downloads/IDE-script&sql/环境变量/env-vars-export-20260515_183238.txt
```

该文件是 Windows 导出的，值末尾带 `\r`，读取时必须去掉：

```bash
env_file='/Users/jason/Downloads/IDE-script&sql/环境变量/env-vars-export-20260515_183238.txt'
for key in ALIBABA_CLOUD_ACCESS_KEY_ID ALIBABA_CLOUD_ACCESS_KEY_SECRET ALIPAY_APP_ID ALIPAY_NOTIFY_URL ALIPAY_PRIVATE_KEY ALIPAY_PUBLIC_KEY; do
  value=$(grep -m1 "^${key}=" "$env_file" | cut -d= -f2- | tr -d '\r')
  if [ -n "$value" ]; then
    export "$key=$value"
  fi
done
```

## 3. 启动服务

### 3.1 启动 MySQL 和 Redis

```bash
brew services start mysql@8.0
brew services start redis
```

验证：

```bash
mysqladmin ping -h 127.0.0.1 -P 3306 -u root
redis-cli -h 127.0.0.1 -p 6379 ping
```

### 3.2 创建数据库（首次）

```bash
mysql -h 127.0.0.1 -u root -e "CREATE DATABASE IF NOT EXISTS community_app DEFAULT CHARACTER SET utf8mb4;"
```

### 3.3 启动后端

```bash
cd /Users/jason/BYSJ/backend
mvn spring-boot:run
```

后端默认端口：`http://127.0.0.1:8080`

验证：

```bash
curl http://127.0.0.1:8080/api/health
```

期望返回：

```json
{"code":0,"message":"ok","data":"ok"}
```

### 3.4 启动 Web 前端

```bash
cd /Users/jason/BYSJ/frontend
npm install
npm run dev
```

前端默认端口：`http://127.0.0.1:5173`

前端通过 Vite 代理访问后端：

```text
/api  -> http://localhost:8080
/ws   -> ws://localhost:8080
```

### 3.5 导入演示数据（可选）

```bash
mysql -h 127.0.0.1 -u root community_app < /Users/jason/BYSJ/backend/src/main/resources/data.sql
```

演示账号：

| 用户名 | 密码 | 手机号 | 角色 |
|---|---|---|---|
| admin | 123456 | 15138114047 | ADMIN |
| employee | 123456 | - | EMPLOYEE |
| user | 123456 | - | USER |

## 4. 后台启动（关终端不停）

### 4.1 后台启动后端

```bash
env_file='/Users/jason/Downloads/IDE-script&sql/环境变量/env-vars-export-20260515_183238.txt'
for key in ALIBABA_CLOUD_ACCESS_KEY_ID ALIBABA_CLOUD_ACCESS_KEY_SECRET ALIPAY_APP_ID ALIPAY_NOTIFY_URL ALIPAY_PRIVATE_KEY ALIPAY_PUBLIC_KEY; do
  value=$(grep -m1 "^${key}=" "$env_file" | cut -d= -f2- | tr -d '\r')
  if [ -n "$value" ]; then export "$key=$value"; fi
done
export COMMUNITY_DB_PASSWORD=
export ALIPAY_ENABLED=true

nohup mvn -f /Users/jason/BYSJ/backend/pom.xml spring-boot:run > /Users/jason/BYSJ/backend.log 2>&1 &
```

### 4.2 后台启动前端

```bash
nohup npm run dev --prefix /Users/jason/BYSJ/frontend -- --host 0.0.0.0 > /Users/jason/BYSJ/frontend.log 2>&1 &
```

### 4.3 查看日志

```bash
tail -f /Users/jason/BYSJ/backend.log
tail -f /Users/jason/BYSJ/frontend.log
```

## 5. 停止服务

```bash
# 停后端
kill $(pgrep -f 'CommunityApplication')

# 停前端
kill $(pgrep -f 'vite.*frontend')

# 停 MySQL 和 Redis
brew services stop mysql@8.0
brew services stop redis
```

## 6. 项目源码修改记录

相比 GitHub 原始仓库，本地做了以下修改：

### 5.1 `backend/src/main/resources/application.yml`

```yaml
# 原始值
scheme-name: ${SMS_SCHEME_NAME:TestScheme}
sign-name: ${SMS_SIGN_NAME:}
template-code: ${SMS_TEMPLATE_CODE:}

# 改为
scheme-name: ${SMS_SCHEME_NAME:测试方案}
sign-name: ${SMS_SIGN_NAME:速通互联验证码}
template-code: ${SMS_TEMPLATE_CODE:100001}
```

### 5.2 `backend/src/main/java/com/community/sms/AliyunSmsGateway.java`

在 `wrapException` 方法末尾增加一行日志：

```java
log.warn("{}: {}", action, message);
```

用于排查阿里云短信调用失败时的具体原因。

## 7. 一键启动脚本（可选）

把以下内容保存为 `start.sh`，放在项目根目录：

```bash
#!/bin/bash
set -e

# 从 Windows 导出文件读取密钥（去掉 \r）
env_file='/Users/jason/Downloads/IDE-script&sql/环境变量/env-vars-export-20260515_183238.txt'
for key in ALIBABA_CLOUD_ACCESS_KEY_ID ALIBABA_CLOUD_ACCESS_KEY_SECRET ALIPAY_APP_ID ALIPAY_NOTIFY_URL ALIPAY_PRIVATE_KEY ALIPAY_PUBLIC_KEY; do
  value=$(grep -m1 "^${key}=" "$env_file" | cut -d= -f2- | tr -d '\r')
  if [ -n "$value" ]; then
    export "$key=$value"
  fi
done

export COMMUNITY_DB_PASSWORD=
export ALIPAY_ENABLED=true

# 启动 MySQL 和 Redis
brew services start mysql@8.0 2>/dev/null || true
brew services start redis 2>/dev/null || true

# 启动后端（后台）
cd /Users/jason/BYSJ/backend
mvn spring-boot:run &
BACKEND_PID=$!

# 等后端就绪
echo "等待后端启动..."
for i in $(seq 1 30); do
  if curl -s http://127.0.0.1:8080/api/health >/dev/null 2>&1; then
    echo "后端已就绪"
    break
  fi
  sleep 1
done

# 启动前端
cd /Users/jason/BYSJ/frontend
npm run dev &

echo ""
echo "后端: http://127.0.0.1:8080"
echo "前端: http://127.0.0.1:5173"
echo ""
echo "按 Ctrl+C 停止所有服务"

wait
```

使用：

```bash
chmod +x /Users/jason/BYSJ/start.sh
/Users/jason/BYSJ/start.sh
```

## 8. 移动端启动与打包说明

### 7.1 移动端有两种运行方式

这个仓库里的 `mobileend` 有两种常用启动方式：

1. 浏览器调试模式
2. Android 原生 App 模式

两者不要混淆。

### 7.2 浏览器调试模式

适合本机先看页面、调接口、调登录流程。

启动命令：

```bash
cd /Users/jason/BYSJ/mobileend
npm install
npm run dev -- --host 0.0.0.0
```

默认端口：

```text
http://127.0.0.1:8100
```

说明：

- `mobileend` 的 Vite 端口是 `8100`
- 浏览器调试模式本质上还是一个前端开发服务器
- 它不是 Android App，只是“移动端页面的 Web 调试版”

### 7.3 浏览器调试模式下的后端地址规则

移动端源码里 REST 和 WebSocket 优先读取环境变量：

- `VITE_API_BASE_URL`
- `VITE_WS_BASE_URL`

如果没有环境变量：

- REST 退回到 `/api`
- WebSocket 退回到当前站点的 `/ws/community`

在本机 Vite 调试模式里，通常建议显式写 `.env`，不要依赖回退逻辑。

### 7.4 本机浏览器调试时推荐的 `.env`

如果只是 Mac 本机浏览器访问移动端页面，推荐：

```env
VITE_API_BASE_URL=http://127.0.0.1:8080/api
VITE_WS_BASE_URL=ws://127.0.0.1:8080
```

这样移动端页面会直接打到本机后端。

### 7.5 Android 模拟器调试时推荐的 `.env`

如果是 Android 模拟器访问本机后端，应使用：

```env
VITE_API_BASE_URL=http://10.0.2.2:8080/api
VITE_WS_BASE_URL=ws://10.0.2.2:8080
```

说明：

- `10.0.2.2` 是 Android 模拟器访问宿主机 `localhost` 的固定地址
- 不要在模拟器里写 `127.0.0.1:8080`

## 9. ngrok 使用说明

### 8.1 什么时候需要 ngrok

如果是下面这些情况，推荐使用 ngrok：

- 真机手机不和 Mac 在同一个局域网
- 演示时不想依赖局域网 IP
- 需要公网可访问地址
- 支付宝回调需要公网地址

### 8.2 ngrok 和局域网 IP 的区别

局域网方式：

- 手机和 Mac 必须在同一 Wi-Fi
- 地址通常是 `http://192.168.x.x:8080`
- 切换 Wi-Fi 后，IP 可能变化

ngrok 方式：

- 手机只要能上网即可
- 不依赖本地局域网 IP
- 免费 ngrok 地址在重开隧道后可能变化

### 8.3 当前这次实际使用的 ngrok 地址

当前后端公网地址：

```text
https://veronika-overclean-charla.ngrok-free.dev
```

它代理到本机：

```text
http://127.0.0.1:8080
```

健康检查：

```bash
curl https://veronika-overclean-charla.ngrok-free.dev/api/health
```

停止 ngrok：

```bash
kill $(pgrep -f ngrok)
```

### 8.4 真机演示时移动端 `.env` 应该怎么写

如果移动端要连当前这条 ngrok 后端，应写：

```env
VITE_API_BASE_URL=https://veronika-overclean-charla.ngrok-free.dev/api
VITE_WS_BASE_URL=wss://veronika-overclean-charla.ngrok-free.dev
```

说明：

- REST 用 `https://.../api`
- WebSocket 用 `wss://...`
- 不要把 WebSocket 写成 `/ws/community` 完整路径，源码会自动补

### 8.5 为什么移动端源码里会兼容 ngrok

`mobileend/src/api/request.js` 里专门做了 ngrok 兼容：

- 识别 `ngrok-free.dev` / `ngrok.io`
- 自动加 `ngrok-skip-browser-warning` 请求头
- 如果网关返回 HTML 页面，会直接提示检查 ngrok 配置

这说明项目本身就考虑过“真机通过 ngrok 连本地后端”的用法。

## 10. Android 打包说明

### 9.1 首次生成 Android 工程

```bash
cd /Users/jason/BYSJ/mobileend
npm install
npm run build
npm run cap:add:android
npm run cap:sync
```

说明：

- `cap:add:android` 只需要首次执行一次
- `cap:sync` 每次前端资源或环境变量变化后都建议执行

### 9.2 Android SDK 默认目录

本机 Android SDK 目录：

```text
/Users/jason/Library/Android/sdk
```

项目 Android SDK 路径文件：

```text
/Users/jason/BYSJ/mobileend/android/local.properties
```

内容：

```properties
sdk.dir=/Users/jason/Library/Android/sdk
```

### 9.3 本次已安装的 Android SDK 组件

当前本机已安装：

- `platform-tools`
- `platforms;android-34`
- `build-tools;34.0.0`
- `build-tools;37.0.0`
- `cmdline-tools;latest`
- `emulator`

说明：

- 当前项目 `compileSdkVersion = 34`
- 所以 `android-34` 和 `build-tools 34.0.0` 已经足够编译
- `build-tools 37.0.0` 是额外安装的更高版本，不影响当前项目

### 9.4 当前项目实际 Android 配置

`mobileend/android/variables.gradle` 当前配置：

```gradle
minSdkVersion = 22
compileSdkVersion = 34
targetSdkVersion = 34
```

### 9.5 当前移动端打包命令

如果已经生成 `android/` 工程，重新打包 debug APK 的命令：

```bash
cd /Users/jason/BYSJ/mobileend/android
./gradlew assembleDebug
```

这条命令是前台编译命令，跑完就结束，不是长期后台进程。

### 9.6 当前已成功生成的 APK

当前 debug APK 路径：

```text
/Users/jason/BYSJ/mobileend/android/app/build/outputs/apk/debug/app-debug.apk
```

也就是：

```text
/Users/jason/BYSJ/mobileend/android/app/build/outputs/apk/debug/app-debug.apk
```

### 9.7 当前这次移动端打包前做过的事情

本次为了让真机通过 ngrok 访问本地后端，已做过这些：

1. 把 `mobileend/.env` 改为：

```env
VITE_API_BASE_URL=https://veronika-overclean-charla.ngrok-free.dev/api
VITE_WS_BASE_URL=wss://veronika-overclean-charla.ngrok-free.dev
```

2. 重新构建前端资源：

```bash
npm run build
```

3. 执行：

```bash
npm run cap:add:android
npm run cap:sync
```

4. 安装 Android SDK 组件

5. 执行：

```bash
./gradlew assembleDebug
```

6. 成功生成 `app-debug.apk`

## 11. 演示时如何选方案

### 10.1 最省事方案

如果只是今天临时演示，优先级建议：

1. 真机 + ngrok
2. 同一局域网 + 局域网 IP
3. Mac 开热点

### 10.2 为什么这次优先选 ngrok

原因：

- 手机不要求和 Mac 在同一个 Wi-Fi
- 不需要记局域网 IP
- 更适合真机演示
- 当前项目移动端源码已经兼容 ngrok

### 10.3 如果以后不用 ngrok

如果以后改回同一局域网访问，把 `mobileend/.env` 改成类似：

```env
VITE_API_BASE_URL=http://192.168.1.xxx:8080/api
VITE_WS_BASE_URL=ws://192.168.1.xxx:8080
```

然后重新：

```bash
npm run build
npm run cap:sync
```

如果只是浏览器调试模式，则重新启动：

```bash
npm run dev -- --host 0.0.0.0
```
