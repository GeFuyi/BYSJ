# BYSJ 启动与部署总说明

本文档汇总本地开发启动和服务器 Docker 部署流程。项目包含：

- `backend`：Spring Boot 后端，提供 REST API 和 WebSocket。
- `frontend`：Vue 3 Web 前端。
- `mobileend`：Ionic Vue 移动端 Web / APK 源码。
- MySQL：保存业务数据。
- Redis：缓存、短信验证码间隔、离线消息等。

## 1. 服务器 Docker 部署说明

### 1.1 当前线上域名和端口

服务器公网入口由宝塔/Nginx 反向代理负责，Docker 容器只监听本机端口。

| 服务 | 公网域名 | 宿主机监听 | 容器内端口 | 说明 |
|---|---|---:|---:|---|
| Web 前端 | `https://frontend.jasonegg.cn` | `127.0.0.1:18080` | `80` | Vue Web 页面 |
| 后端 | `https://backend.jasonegg.cn` | `127.0.0.1:18081` | `8080` | Spring Boot API / WebSocket |
| 移动端 Web | `https://mobileend.jasonegg.cn` | `127.0.0.1:18082` | `80` | Ionic Vue 页面 |
| MySQL | 无公网入口 | 不映射 | `3306` | Docker 内部 `mysql:3306` |
| Redis | 无公网入口 | 不映射 | `6379` | Docker 内部 `redis:6379` |

宝塔反向代理配置：

```text
frontend.jasonegg.cn   -> http://127.0.0.1:18080
backend.jasonegg.cn    -> http://127.0.0.1:18081
mobileend.jasonegg.cn  -> http://127.0.0.1:18082
```

后端公网访问地址：

```text
https://backend.jasonegg.cn/api/health
https://backend.jasonegg.cn/api/...
wss://backend.jasonegg.cn/ws/community
```

真实手机或浏览器不能访问服务器内部的 `127.0.0.1:18081`。真机 APK 打包时应使用：

```env
VITE_API_BASE_URL=https://backend.jasonegg.cn/api
VITE_WS_BASE_URL=wss://backend.jasonegg.cn
```

### 1.2 Docker Compose 服务结构

服务器使用项目根目录的 `docker-compose.yml` 启动五个服务：

```text
mysql      MySQL 8.0，内部地址 mysql:3306
redis      Redis 7，内部地址 redis:6379
backend    Spring Boot，内部端口 8080，对宿主机 127.0.0.1:18081
frontend   Vue Web，内部端口 80，对宿主机 127.0.0.1:18080
mobileend  Ionic Vue Web，内部端口 80，对宿主机 127.0.0.1:18082
```

持久化数据：

```text
bysj_mysql_data   MySQL 数据卷
bysj_redis_data   Redis 数据卷
./data/uploads    后端上传文件目录
```

MySQL 和 Redis 不映射公网端口，不会占用服务器已有的 `3306`、`6379`。

### 1.3 首次部署命令

进入服务器：

```bash
ssh root@192.204.35.185
```

如果服务器已经用 HTTPS 拉过仓库：

```bash
cd /www/wwwroot/BYSJ
git pull origin main
```

如果服务器还没有项目目录：

```bash
mkdir -p /www/wwwroot
cd /www/wwwroot
git clone https://github.com/GeFuyi/BYSJ.git BYSJ
cd /www/wwwroot/BYSJ
```

确认端口未被占用：

```bash
ss -lntp | grep -E ':18080|:18081|:18082' || true
```

创建 `.env`。如果已经有 `.env`，不要覆盖；如果是首次部署，可以从示例复制：

```bash
cp .env.example .env
chmod 600 .env
nano .env
```

当前 `.env` 格式如下：

```env
MYSQL_ROOT_PASSWORD=change-this-root-password
MYSQL_DATABASE=community_app
MYSQL_USER=community_user
MYSQL_PASSWORD=change-this-app-password

COMMUNITY_JWT_SECRET=change-this-to-a-long-random-secret-at-least-32-chars
ALIPAY_ENABLED=false
JAVA_OPTS=

FRONTEND_HTTP_PORT=18080
BACKEND_HTTP_PORT=18081
MOBILEEND_HTTP_PORT=18082

MOBILEEND_API_BASE_URL=/api
MOBILEEND_WS_BASE_URL=
```

构建并启动：

```bash
docker compose build
docker compose up -d
docker compose ps
```

### 1.4 服务器验证命令

后端本机验证：

```bash
curl http://127.0.0.1:18081/api/health
```

期望返回：

```json
{"code":0,"message":"ok","data":"ok"}
```

前端和移动端本机验证：

```bash
curl -I http://127.0.0.1:18080
curl -I http://127.0.0.1:18082
```

公网验证：

```bash
curl https://backend.jasonegg.cn/api/health
curl -I https://frontend.jasonegg.cn
curl -I https://mobileend.jasonegg.cn
```

浏览器访问：

```text
https://frontend.jasonegg.cn
https://mobileend.jasonegg.cn
https://backend.jasonegg.cn/api/health
```

演示账号：

| 用户名 | 密码 | 手机号 | 角色 |
|---|---|---|---|
| admin | 123456 | 15138114047 | ADMIN |
| employee | 123456 | 15138114047 | EMPLOYEE |
| user | 123456 | 15138114047 | USER |

### 1.5 日志和排错

查看容器状态：

```bash
cd /www/wwwroot/BYSJ
docker compose ps
```

查看后端日志：

```bash
docker compose logs backend --tail=200
```

持续查看后端日志：

```bash
docker compose logs -f backend
```

查看所有服务日志：

```bash
docker compose logs -f
```

常用排错命令：

```bash
docker compose logs mysql --tail=100
docker compose logs redis --tail=100
docker compose logs frontend --tail=100
docker compose logs mobileend --tail=100
```

如果刚启动时 `curl` 出现 `Connection reset by peer`，通常是 Spring Boot 还没完全启动，等待 10 秒后重试：

```bash
curl -v http://127.0.0.1:18081/api/health
```

### 1.6 后续更新部署

本地推送新代码后，服务器执行：

```bash
cd /www/wwwroot/BYSJ
git pull origin main
docker compose build
docker compose up -d
docker compose ps
curl http://127.0.0.1:18081/api/health
```

如果只改后端，也可以只构建后端：

```bash
cd /www/wwwroot/BYSJ
git pull origin main
docker compose build backend
docker compose up -d
docker compose ps
curl http://127.0.0.1:18081/api/health
```

### 1.7 停止、重启和重置

停止整套服务：

```bash
cd /www/wwwroot/BYSJ
docker compose down
```

重启服务：

```bash
cd /www/wwwroot/BYSJ
docker compose up -d
```

重启某个服务：

```bash
docker compose restart backend
docker compose restart frontend
docker compose restart mobileend
```

彻底重置数据库和 Redis 数据：

```bash
cd /www/wwwroot/BYSJ
docker compose down
docker volume rm bysj_mysql_data bysj_redis_data
docker compose up -d
```

注意：重置 volume 会删除数据库数据。

### 1.8 宝塔/Nginx 注意事项

三个站点都建议开启 SSL，并开启 HTTP 到 HTTPS 重定向。

`backend.jasonegg.cn` 必须支持 WebSocket Upgrade。Nginx 核心配置如下：

```nginx
proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
```

如果 `frontend.jasonegg.cn` 或 `mobileend.jasonegg.cn` 页面内使用同域 `/ws`，对应站点也要支持 WebSocket Upgrade。

## 2. 本地开发启动说明

### 2.1 前置环境

| 组件 | 版本 | 安装方式 |
|---|---|---|
| JDK | 17 | `brew install openjdk@17` |
| Maven | 3.9+ | `brew install maven` |
| MySQL | 8.0 | `brew install mysql@8.0` |
| Redis | 7+ | `brew install redis` |
| Node.js | 18+ | 已有 |
| npm | 9+ | 已有 |

### 2.2 环境变量

Java / Maven：

```bash
export PATH="/opt/homebrew/bin:/opt/homebrew/opt/openjdk@17/bin:$PATH"
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"
```

数据库密码。本地 Homebrew MySQL root 默认空密码时：

```bash
export COMMUNITY_DB_PASSWORD=
```

如果本地 MySQL root 有密码，填实际密码：

```bash
export COMMUNITY_DB_PASSWORD=你的MySQL密码
```

支付宝本地不需要时：

```bash
export ALIPAY_ENABLED=false
```

阿里云短信需要真实发送时再配置：

```bash
export ALIBABA_CLOUD_ACCESS_KEY_ID=你的阿里云AccessKeyID
export ALIBABA_CLOUD_ACCESS_KEY_SECRET=你的阿里云AccessKeySecret
export SMS_SIGN_NAME=你的签名
export SMS_TEMPLATE_CODE=你的模板Code
```

### 2.3 启动 MySQL 和 Redis

```bash
brew services start mysql@8.0
brew services start redis
```

验证：

```bash
mysqladmin ping -h 127.0.0.1 -P 3306 -u root
redis-cli -h 127.0.0.1 -p 6379 ping
```

创建数据库：

```bash
mysql -h 127.0.0.1 -u root -e "CREATE DATABASE IF NOT EXISTS community_app DEFAULT CHARACTER SET utf8mb4;"
```

### 2.4 启动后端

```bash
cd /Users/jason/BYSJ/backend
mvn spring-boot:run
```

后端默认端口：

```text
http://127.0.0.1:8080
```

验证：

```bash
curl http://127.0.0.1:8080/api/health
```

期望返回：

```json
{"code":0,"message":"ok","data":"ok"}
```

### 2.5 启动 Web 前端

```bash
cd /Users/jason/BYSJ/frontend
npm install
npm run dev
```

Web 前端默认端口：

```text
http://127.0.0.1:5173
```

开发环境代理：

```text
/api -> http://localhost:8080
/ws  -> ws://localhost:8080
```

### 2.6 启动移动端 Web

```bash
cd /Users/jason/BYSJ/mobileend
npm install
npm run dev
```

移动端 Web 默认端口：

```text
http://127.0.0.1:8100
```

本地 `.env` 示例：

```env
VITE_API_BASE_URL=http://127.0.0.1:8080/api
VITE_WS_BASE_URL=ws://127.0.0.1:8080
```

Android 模拟器访问本机后端时可使用：

```env
VITE_API_BASE_URL=http://10.0.2.2:8080/api
VITE_WS_BASE_URL=ws://10.0.2.2:8080
```

真机或线上 APK 使用：

```env
VITE_API_BASE_URL=https://backend.jasonegg.cn/api
VITE_WS_BASE_URL=wss://backend.jasonegg.cn
```

### 2.7 导入演示数据

后端启动时会执行 `schema.sql` 建表和补列逻辑。需要演示账号时可导入：

```bash
mysql -h 127.0.0.1 -u root community_app < /Users/jason/BYSJ/backend/src/main/resources/data.sql
```

演示账号同服务器部署部分。

### 2.8 本地停止服务

```bash
# 停后端
kill $(pgrep -f 'CommunityApplication')

# 停 Web 前端
kill $(pgrep -f 'vite.*frontend')

# 停移动端 Web
kill $(pgrep -f 'vite.*mobileend')

# 停 MySQL 和 Redis
brew services stop mysql@8.0
brew services stop redis
```

## 3. 本地 Docker 验证

如果本地 Docker Desktop 已启动，也可以在本机验证服务器同款部署：

```bash
cd /Users/jason/BYSJ
cp .env.example .env
docker compose build
docker compose up -d
docker compose ps
curl http://127.0.0.1:18081/api/health
```

访问：

```text
http://127.0.0.1:18080
http://127.0.0.1:18082
http://127.0.0.1:18081/api/health
```

停止本地 Docker：

```bash
docker compose down
```

## 4. 构建命令汇总

后端打包：

```bash
mvn -f /Users/jason/BYSJ/backend/pom.xml -DskipTests package
```

Web 前端构建：

```bash
npm --prefix /Users/jason/BYSJ/frontend run build
```

移动端 Web 构建：

```bash
npm --prefix /Users/jason/BYSJ/mobileend run build
```

服务器 Docker 构建：

```bash
docker compose build
```

## 5. Git 和安全注意事项

不要提交以下内容：

```text
.env
data/
backend/target/
frontend/dist/
mobileend/dist/
node_modules/
*.log
```

这些已经由 `.gitignore` 忽略。

提交代码前检查：

```bash
git status
git diff --check
```

服务器 `.env` 只保留在服务器本地，不上传 GitHub。
